# Ubuntu/Linux 服务器部署指南

> ⚠️ **注意**：本文档以 `/www/wwwroot/luckinhappykami` 为项目路径示例。如果您的实际路径不同（如 `/www/wwwroot/kami`），请将文档中所有路径替换为您的实际路径。

## 目录

- [环境要求](#环境要求)
- [第一步：上传项目文件](#第一步上传项目文件)
- [第二步：清理旧依赖并重新安装](#第二步清理旧依赖并重新安装)
- [第三步：修复 tsconfig.json 配置](#第三步修复-tsconfigjson-配置)
- [第四步：配置环境变量](#第四步配置环境变量)
- [第五步：初始化数据库](#第五步初始化数据库)
- [第六步：生产模式构建与启动](#第六步生产模式构建与启动)
- [第七步：设置开机自启（PM2）](#第七步设置开机自启pm2)
- [第八步：配置 Nginx 反向代理（推荐）](#第八步配置-nginx-反向代理推荐)
- [第九步：配置防火墙](#第九步配置防火墙)
- [常见问题排查](#常见问题排查)

---

## 环境要求

```bash
# Node.js >= 18
node -v

# npm >= 9
npm -v
```

如未安装 Node.js，可使用以下命令：

```bash
# 方式一：nvm 安装（推荐）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install 18
nvm use 18

# 方式二：直接安装
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

---

## 第一步：上传项目文件

```bash
# 将项目上传到服务器（路径可自定义，建议 /www/wwwroot/luckinhappykami）
# 使用 scp、rsync 或宝塔面板上传均可

# 进入项目目录
cd /www/wwwroot/luckinhappykami
```

> ⚠️ **重要**：上传时 **不要包含** `node_modules`、`dist`、`frontend/dist` 目录！
> 跨平台复制 `node_modules` 会导致原生二进制文件（如 esbuild、sqlite3、bcrypt）无法运行。

应上传的文件/目录：
- `src/`
- `frontend/src/`
- `frontend/package.json`
- `frontend/package-lock.json`
- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `frontend/vite.config.ts`
- `ecosystem.config.js`（PM2 配置）
- `nginx.conf.example`（Nginx 配置模板）
- `install.sh`
- `.env.example`
- 其他配置文件

---

## 第二步：清理旧依赖并重新安装

```bash
cd /www/wwwroot/luckinhappykami

# 1. 删除可能存在的旧 node_modules（如果上传时误带了）
rm -rf node_modules
rm -rf frontend/node_modules

# 2. 清理旧的构建产物
rm -rf dist
rm -rf frontend/dist

# 3. 设置国内 npm 镜像（加速下载）
npm config set registry https://registry.npmmirror.com

# 4. 安装后端依赖
npm install

# 5. 安装前端依赖
cd frontend && npm install && cd ..
```

---

## 第三步：修复 tsconfig.json 配置

项目 `tsconfig.json` 中引用了未安装的 `jest` 类型，会导致 TypeScript 编译失败。

**错误信息**：
```
error TS2688: Cannot find type definition file for 'jest'.
```

**修复方法**（三选一）：

### 方法一：Python 脚本（推荐）

```bash
cd /www/wwwroot/luckinhappykami

python3 -c "
import json
with open('tsconfig.json') as f:
    data = json.load(f)
data['compilerOptions']['types'] = ['node']
with open('tsconfig.json', 'w') as f:
    json.dump(data, f, indent=2)
print('Done!')
"
```

### 方法二：sed 命令

```bash
cd /www/wwwroot/luckinhappykami
sed -i 's/\["node", "jest"\]/["node"]/' tsconfig.json
```

### 方法三：手动编辑

```bash
nano tsconfig.json
# 找到 "types": ["node", "jest"]，改为 "types": ["node"]
# Ctrl+O 保存，Ctrl+X 退出
```

**验证修改**：
```bash
grep '"types"' tsconfig.json
# 应输出: "types": ["node"]
```

---

## 第四步：配置环境变量

```bash
cd /www/wwwroot/luckinhappykami

# 复制示例配置
cp .env.example .env

# 编辑配置
nano .env
```

**关键配置项**：

```env
# ===========================================
# 服务端配置
# ===========================================

# 服务端口
# ┌─ 使用 Nginx 反代时：设为 3001，并同步修改 ecosystem.config.js 和 nginx.conf
# └─ 不使用 Nginx 时：设为 3000，直接对外暴露
# 三种配置必须保持一致：.env / ecosystem.config.js / nginx.conf
PORT=3000

# 运行环境: development | production
NODE_ENV=production

# ===========================================
# 数据库配置
# ===========================================

# SQLite 数据库文件路径
DB_PATH=./data/luckinhappykami.db

# ===========================================
# JWT 配置
# ===========================================

# JWT 密钥（必须修改！用以下命令生成）
# 生成命令：openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT token 过期时间
JWT_EXPIRES_IN=24h

# ===========================================
# 其他配置
# ===========================================

# 上传目录
UPLOAD_PATH=./uploads

# 最大上传文件大小（2MB）
MAX_FILE_SIZE=2097152
```

生成 JWT 密钥：
```bash
openssl rand -hex 32
# 将输出的字符串替换到 JWT_SECRET
```

---

## 第五步：初始化数据库

```bash
cd /www/wwwroot/luckinhappykami

# 创建必要目录
mkdir -p data uploads

# 初始化数据库
npm run init-db
```

应看到类似输出：
```
✅ 数据库初始化完成
✅ 已创建默认管理员账号
```

---

## 第六步：生产模式构建与启动

### 构建

```bash
cd /www/wwwroot/luckinhappykami

# 一键构建（前端打包 + 后端编译 + 复制静态文件）
npm run build
```

构建过程：
1. `npm run build:client` — 前端打包为静态文件 → `frontend/dist/`
2. `npm run build:server` — 后端 TypeScript 编译 → `dist/`
3. 复制 `frontend/dist/*` → `dist/public/`

### 启动（临时测试）

```bash
# 生产模式启动（仅后端，自动托管前端静态文件）
npm start
```

启动成功标志：
```
🚀 LuckinHappy Card Verification System v1.0.0
📡 服务器运行在 http://0.0.0.0:3000
🌍 环境: production
📊 数据库: ./data/luckinhappykami.db
✅ 数据库连接成功，已启用性能优化
```

浏览器访问 `http://服务器IP:3000` 即可测试。
测试无误后 `Ctrl+C` 停止，继续下一步配置。

---

## 第七步：设置开机自启（PM2）

```bash
# 全局安装 PM2
npm install -g pm2

# 修改 ecosystem.config.js 中的端口（如果需要）
# 默认配置使用 3001 端口，配合 Nginx 使用
# 如果不使用 Nginx，将端口改为 3000 并修改 .env 中的 PORT

# 启动应用
cd /www/wwwroot/luckinhappykami
pm2 start ecosystem.config.js

# 查看运行状态
pm2 list

# 查看实时日志
pm2 logs luckinhappykami

# 停止应用
pm2 stop luckinhappykami

# 重启应用
pm2 restart luckinhappykami

# 设置开机自启
pm2 save
pm2 startup systemd
# 复制输出的命令并执行（如：sudo env PATH=$PATH:/usr/bin /usr/local/lib/node_modules/pm2/bin/pm2 startup systemd -u root --hp /root）

# 验证开机自启
pm2 resurrect
```

**PM2 配置说明**：

[ecosystem.config.js](file:///Users/lihao/Documents/GitHub/luckinhappykami/ecosystem.config.js) 默认配置：
- 应用名：`luckinhappykami`
- 端口：`3001`（配合 Nginx 反向代理使用）
- 环境：`production`
- 内存上限：`1G`

### 端口对齐原则

> ⚠️ **重要**：以下三处端口必须保持一致，否则会出现连接失败！
>
> | 配置文件 | 默认端口 | 说明 |
> |---------|---------|------|
> | `.env` | 3000 | 应用实际监听端口 |
> | `ecosystem.config.js` | 3001 | PM2 启动时的 PORT 环境变量 |
> | `nginx.conf` | 3001 | Nginx proxy_pass 转发目标 |

**方案一：使用 Nginx 反代（推荐）**
1. 修改 `.env`：`PORT=3001`
2. 修改 `ecosystem.config.js`：`PORT: 3001`（已是默认值，无需改）
3. Nginx 配置：`proxy_pass http://127.0.0.1:3001`（已是默认值，无需改）

**方案二：不使用 Nginx，直接对外暴露**
1. 修改 `.env`：`PORT=3000`（已是默认值，无需改）
2. 修改 `ecosystem.config.js`：将 `PORT: 3001` 改为 `PORT: 3000`
3. 防火墙开放 3000 端口：`sudo ufw allow 3000`

---

## 第八步：配置 Nginx 反向代理（推荐）

使用 Nginx 反向代理可以：
- 通过 80/443 端口对外服务
- 提供静态文件缓存，提升性能
- 启用 Gzip 压缩
- 配置 SSL/HTTPS
- 隐藏后端端口

### 安装 Nginx

```bash
sudo apt-get update
sudo apt-get install -y nginx
```

### 配置 Nginx

```bash
# 复制项目自带的配置模板
cp /www/wwwroot/luckinhappykami/nginx.conf.example /etc/nginx/sites-available/luckinhappykami

# 修改配置
sudo nano /etc/nginx/sites-available/luckinhappykami
```

**需要修改的关键项**：

```nginx
server {
    listen 80;
    # 修改为您的域名或服务器 IP
    server_name your-domain.com;

    # 修改为实际的前端构建产物路径
    root /www/wwwroot/luckinhappykami/dist/public;

    # API 代理到后端（PM2 配置的 3001 端口）
    location /api {
        proxy_pass http://127.0.0.1:3001;
        # ... 其余配置保持不变
    }

    # HTTPS 配置（可选，取消注释并配置 SSL 证书）
    # 参见 nginx.conf.example 底部的 HTTPS 配置
}
```

### 启用并测试

```bash
# 创建软链接启用站点
sudo ln -s /etc/nginx/sites-available/luckinhappykami /etc/nginx/sites-enabled/

# 移除默认站点（可选）
sudo rm /etc/nginx/sites-enabled/default

# 测试配置是否正确
sudo nginx -t

# 重载 Nginx
sudo systemctl reload nginx

# 查看 Nginx 状态
sudo systemctl status nginx
```

### Nginx 常用命令

```bash
sudo systemctl start nginx    # 启动
sudo systemctl stop nginx     # 停止
sudo systemctl reload nginx   # 重载配置
sudo systemctl restart nginx  # 重启
sudo systemctl status nginx   # 查看状态
```

---

## 第九步：配置防火墙

```bash
# 查看防火墙状态
sudo ufw status

# 启用防火墙（如未启用）
sudo ufw enable

# 允许 SSH 登录（非常重要！否则会被锁在服务器外）
sudo ufw allow 22

# 允许 HTTP（Nginx 对外服务）
sudo ufw allow 80

# 允许 HTTPS（如配置了 SSL）
sudo ufw allow 443

# 如不使用 Nginx 直接对外，开放应用端口
# sudo ufw allow 3000
# 或如果使用 Nginx + PM2 3001 端口：
# sudo ufw allow 3001  # 仅内部网络需要

# 重新加载规则
sudo ufw reload

# 验证规则
sudo ufw status
```

---

## 常见问题排查

### 1. esbuild 平台二进制不匹配

**错误信息**：
```
You installed esbuild for another platform than the one you're currently using.
Specifically the "@esbuild/win32-x64" package is present but this platform
needs the "@esbuild/linux-x64" package instead.
```

**原因**：`node_modules` 是从 Windows/macOS 复制过来的。

**解决**：
```bash
cd /www/wwwroot/luckinhappykami/frontend
rm -rf node_modules
npm install
```

### 2. TypeScript 编译失败：找不到 jest 类型

**错误信息**：
```
error TS2688: Cannot find type definition file for 'jest'.
```

**解决**：参见 [第三步：修复 tsconfig.json 配置](#第三步修复-tsconfigjson-配置)

### 3. 端口被占用

```bash
# 查看端口占用
lsof -i :3000
lsof -i :3001
lsof -i :80

# 杀掉占用进程
kill -9 <PID>
```

### 4. 数据库权限问题

```bash
# 确保 data 和 uploads 目录可写
chmod 755 /www/wwwroot/luckinhappykami/data
chmod 755 /www/wwwroot/luckinhappykami/uploads

# 如果使用 PM2 以 root 以外用户运行
# 需要修改目录所有者
chown -R nodejs:nodejs /www/wwwroot/luckinhappykami/data
```

### 5. Nginx 502 Bad Gateway

**原因**：后端应用未启动或端口不匹配。

```bash
# 检查 PM2 应用状态
pm2 list

# 检查后端日志
pm2 logs luckinhappykami

# 确认端口一致
# PM2 配置的端口（默认 3001）需与 Nginx proxy_pass 端口一致
```

### 6. Nginx 无法加载前端页面

**原因**：前端未构建或路径错误。

```bash
# 重新构建前端
cd /www/wwwroot/luckinhappykami
npm run build

# 确认静态文件存在
ls -la dist/public/

# 确认 Nginx root 路径正确
cat /etc/nginx/sites-enabled/luckinhappykami | grep root
```

### 7. 开发模式启动（仅用于调试）

```bash
cd /www/wwwroot/luckinhappykami
npm run dev
```

> ⚠️ 开发模式会同时启动后端（nodemon）和前端（vite dev server），**不可用于生产环境**。

### 8. 宝塔面板用户注意事项

如果使用宝塔面板：
- 开发模式：使用面板的「网站」功能，启动命令选择 `npm run dev`
- 生产模式：按本文档步骤部署，面板仅用于管理 Nginx

---

## 完整部署流程 Checklist

- [ ] **环境准备**
  - [ ] Node.js >= 18 已安装
  - [ ] npm >= 9 已安装
  - [ ] Nginx 已安装（推荐）

- [ ] **项目部署**
  - [ ] 项目文件已上传（不含 node_modules）
  - [ ] 后端依赖已安装（`npm install`）
  - [ ] 前端依赖已安装（`cd frontend && npm install`）
  - [ ] `tsconfig.json` 已修复（移除 jest 类型）

- [ ] **配置**
  - [ ] `.env` 已配置（JWT_SECRET 已修改）
  - [ ] 端口已确定（推荐 3001 + Nginx 80）
  - [ ] 数据库已初始化（`npm run init-db`）

- [ ] **构建与启动**
  - [ ] 构建成功（`npm run build`）
  - [ ] 测试启动成功（`npm start` 测试）
  - [ ] PM2 配置完成（`pm2 start ecosystem.config.js`）
  - [ ] 开机自启已设置（`pm2 startup systemd`）

- [ ] **Nginx 反向代理**
  - [ ] 配置文件已创建（`/etc/nginx/sites-available/luckinhappykami`）
  - [ ] 路径和端口已检查
  - [ ] 配置测试通过（`nginx -t`）
  - [ ] Nginx 已重载（`nginx -s reload`）

- [ ] **安全**
  - [ ] 防火墙已配置（仅开放 22/80/443）
  - [ ] HTTPS 已启用（生产环境推荐）
  - [ ] SSL 证书已配置（推荐）